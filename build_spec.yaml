version: 0.1

phases:
  pre_build:
    commands:
      - echo "Logging into OCIR (gestiona OCI DevOps)"
  build:
    commands:
      - cd MtdrSpring/backend
      - docker build -t iad.ocir.io/axm8kysebnqa/todolistapp-springboot:${image_tag} -f Dockerfile .
      - docker push iad.ocir.io/axm8kysebnqa/todolistapp-springboot:${image_tag}
